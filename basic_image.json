{
  "builders": [
  {
    "type": "openstack",
    "source_image": "cirros",
    "ssh_username": "cirros",
    "openstack_provider": "nova",
    "image_name": "cirros-gold",
    "networks": "private",
    "security_groups": "default",
    "flavor": "tempest_1"
  }
  ],
  "provisioners": [
  {
    "type": "shell",
    #"script": "local_path_to_script/script.sh",
    #"pause_before": "10s",
    #"execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E {{ .Path }}"
    "execute_command": "touch /coco.txt"
  }
  ]
}
